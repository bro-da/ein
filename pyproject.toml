[project]
name = "ein"
version = "0.1.0"
description = "command builder with fzf with plugins"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer>=0.20.0",
]
[project.scripts]
ein = "ein.__main__:main"

[tool.uv]
package = true